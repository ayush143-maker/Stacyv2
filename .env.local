# Server-side only. Read exclusively by api/chat.js (and by
# server.dev.js for local development). NEVER prefix this with VITE_ —
# any VITE_-prefixed variable gets bundled into client JavaScript and
# would leak the key to every visitor's browser.
GEMINI_API_KEY=your-gemini-api-key-here
